# Release Notes - 2026-07-07

## Fixed

- Fixed `DecoyPYratPlus/decoyPYratPlus.py` imports so the main CLI works when executed as a package module instead of only in direct-script contexts.
- Updated the conda recipe in `meta.yaml` to point at the renamed `DecoyPYratPlus` package and to include the required `numpy` dependency.

## Changed

- Expanded `.gitignore` to cover Python caches, build artifacts, virtual environments, editor files, and local packaging output.
- Rewrote `README.md` to match the current package layout, installation flow, CLI entry points, and major runtime options.
